import { render, screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,it,expect,vi } from "vitest"; import { WorksheetBuilder } from "./WorksheetBuilder";
describe("WorksheetBuilder",()=>{it("validates configuration and previews then approves a draft",async()=>{const user=userEvent.setup();const generate=vi.fn().mockResolvedValue({id:1,status:"SUCCEEDED",message:"Ready",worksheet:{id:9,title:"Water drill",instructions:null,targetMode:"CLASS",status:"DRAFT",dueAt:null,questions:[{id:2,code:"Q",prompt:"Explain evaporation.",totalMarks:2,questionType:"OPEN_ENDED",topicName:"Water"}]}});const approve=vi.fn().mockResolvedValue({id:9,title:"Water drill",instructions:null,targetMode:"CLASS",status:"APPROVED",dueAt:null,questions:[]});render(<WorksheetBuilder classId={1} generate={generate} approve={approve}/>);await user.click(screen.getByRole("button",{name:"Generate worksheet draft"}));expect(screen.getByRole("alert")).toBeVisible();await user.type(screen.getByLabelText(/Syllabus topic IDs/),"3");await user.click(screen.getByRole("button",{name:"Generate worksheet draft"}));expect(await screen.findByText("Explain evaporation.",{exact:false})).toBeVisible();await user.click(screen.getByRole("button",{name:"Approve & assign worksheet"}));expect(approve).toHaveBeenCalledWith(9);});});
