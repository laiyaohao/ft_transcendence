import { render, screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe, expect, it, vi } from "vitest"; import Page from "./page";
vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn() }), useSearchParams: () => new URLSearchParams() }));
describe("Upload wizard", () => { it("requires pages, accepts a file and exposes camera input", async () => { vi.stubGlobal("URL", { createObjectURL: vi.fn(() => "blob:file"), revokeObjectURL: vi.fn() }); render(<Page />); const user = userEvent.setup(); await user.click(screen.getByRole("button", { name: "Continue" })); await user.click(screen.getByRole("button", { name: "Review submission" })); expect(screen.getByRole("alert")).toHaveTextContent(/Add at least one page/); const input = document.querySelector('input[type="file"][multiple]') as HTMLInputElement; await user.upload(input, new File(["page"], "page.jpg", { type: "image/jpeg" })); expect(screen.getByText("1 PAGE DETECTED")).toBeVisible(); expect(document.querySelector('input[capture="environment"]')).toBeTruthy(); }); });
